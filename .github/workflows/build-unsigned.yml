name: Build Unsigned (All Platforms)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # 1. Android (Kotlin + CMake)
  build-android:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
    - name: Build Debug APK
      run: |
        # cd src/platforms/android
        # ./gradlew assembleDebug --no-daemon
        echo "Placeholder for Android build"
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: wangwangji-android-debug
        # path: src/platforms/android/app/build/outputs/apk/debug/app-debug.apk
        path: README.md # Placeholder

  # 2. iOS (Swift + CMake) - Unsigned .app
  build-ios:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build Unsigned .app
      run: |
        # cd src/platforms/ios
        # xcodebuild -project WangWangJi.xcodeproj ...
        echo "Placeholder for iOS build"
    - name: Upload .app
      uses: actions/upload-artifact@v4
      with:
        name: wangwangji-ios-unsigned
        # path: src/platforms/ios/build/Debug-iphoneos/WangWangJi.app
        path: README.md # Placeholder

  # 3. Windows (WinUI 3 + CMake)
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.1
    - name: Build Solution
      run: |
        # cd src/platforms/windows
        # msbuild WangWangJi.sln ...
        echo "Placeholder for Windows build"
    - name: Upload EXE
      uses: actions/upload-artifact@v4
      with:
        name: wangwangji-windows-debug
        # path: src/platforms/windows/x64/Debug/WangWangJi.exe
        path: README.md # Placeholder

  # 4. Web (WASM + CMake)
  build-web:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup Emscripten
      uses: mymindstorm/setup-emsdk@v14
    - name: Build WASM
      run: |
        # mkdir -p build-web
        # cd build-web
        # emcmake cmake ../src/core
        # emmake make
        echo "Placeholder for Web build"
    - name: Upload WASM
      uses: actions/upload-artifact@v4
      with:
        name: wangwangji-web-wasm
        # path: build-web/*.wasm
        path: README.md # Placeholder